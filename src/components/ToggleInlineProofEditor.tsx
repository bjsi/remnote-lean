import { usePlugin, useRunAsync, WidgetLocation } from '@remnote/plugin-sdk';

interface ToggleInlineProofEditorProps {
  isOpen: boolean;
}

export const ToggleInlineProofEditor = (props: ToggleInlineProofEditorProps) => {
  return (
  );
};
